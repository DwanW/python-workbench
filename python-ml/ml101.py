# import data
# clean data (manage missing labels and field, so prediction is more accurate)
# split data (divide into training set and test set, so human can determine if model is accurate)
# create a model (import algorithms)
# check output of the model with test set()
# improve
